# PostgreSQL Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/expense_tracker

# JWT Configuration
JWT_SECRET=your_jwt_secret_here_make_it_long_and_complex

# Client Configuration
CLIENT_URL=http://localhost:5173

# Server Configuration
NODE_ENV=development
PORT=8000

# Example PostgreSQL connection strings:
# Local PostgreSQL: postgresql://username:password@localhost:5432/expense_tracker
# Heroku Postgres: postgres://username:password@hostname:5432/database_name
# Railway/Render: postgresql://username:password@hostname:port/database_name

# Instructions:
# 1. Replace 'username' and 'password' with your PostgreSQL credentials
# 2. Replace 'expense_tracker' with your database name
# 3. If using a cloud service, replace the entire DATABASE_URL with your provider's connection string
# 4. Generate a strong JWT_SECRET for production use